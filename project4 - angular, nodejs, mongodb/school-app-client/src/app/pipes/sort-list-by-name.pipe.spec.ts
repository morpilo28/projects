import { SortListByNamePipe } from './sort-list-by-name.pipe';

describe('SortListByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortListByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
