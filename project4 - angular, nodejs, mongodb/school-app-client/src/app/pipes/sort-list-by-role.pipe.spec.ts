import { SortListByRolePipe } from './sort-list-by-role.pipe';

describe('SortListByRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SortListByRolePipe();
    expect(pipe).toBeTruthy();
  });
});
